## Carbon Daemon Utility

A reference sheet for the `carbon.daemon` CLI tool.

### --start
Start the daemon.
```sh
carbon.daemon --start
```

### --restart
Restart the daemon.
```sh
carbon.daemon --restart
```

### --end
Kill the daemon.
```sh
carbon.daemon --end
```